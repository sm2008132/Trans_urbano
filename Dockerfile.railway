FROM node:22-alpine

WORKDIR /app

# Copiar todo el proyecto
COPY . .

# Instalar dependencias del backend
WORKDIR /app/backend
RUN npm ci --only=production

# Build
RUN npm run build

# Expose puerto
EXPOSE 3000

# Start
CMD ["npm", "start"]
